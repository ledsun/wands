## Echo Server Example

### Usage

Install the required gems:

```shell
bundle update
```

Start the echo server:

```shell
./echo_server.rb
```

Open the index.html file in a browser:

```shell
open index.html
```
